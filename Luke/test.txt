import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class Easy4 {
public static void main(String[] args) {

		try {
			Scanner scanner = new Scanner(System.in);
			List<String> inputList = new ArrayList<>();
			String line;
			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}
			scanner.close();
			String[] inputArray = new String[inputList.size()];
			inputArray = inputList.toArray(inputArray);
			
			System.out.println(inputArray[0]);
			System.out.println(inputArray[1]);
			
			String[] strSplit = inputArray[0].split(":");
			
			
			int addedNum = Integer.parseInt(inputArray[1]);
			int hours = Integer.parseInt(strSplit[0]);
			int minutes = Integer.parseInt(strSplit[1]);
                  if (addedNum < 60 & (addedNum + minutes) < 60) {
	                 minutes = minutes + addedNum;
                        } else if (addedNum >= 60 & (addedNum + minutes) < 60) {
                        	minutes = minutes + (addedNum - (60 - minutes));
                        	hours = hours + 1;
                        } else if (addedNum < 60 & (addedNum + minutes) >= 60){
                        	minutes = minutes + (addedNum - (60 - minutes));
                        	hours = hours + 1;
                        }
                        else if (addedNum >= 60 & (addedNum + minutes) >= 60) {
                        	minutes = minutes + (addedNum - (120 - minutes));
                        	hours = hours + 2;
                        }
			System.out.println(hours + ":" + minutes);

			

		} catch (Exception e) {
			e.printStackTrace();
		}
	}

}


import java.util.Set;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Map;

public class Hash {
	public static void main(String[] args) {
		Set<String> words = new HashSet<>();
	words.add("disallow");
	words.add("discourse");
	words.add("fidelity");
	words.add("muffler");
	words.add("limn");
	words.add("myriad");
	words.add("disconcert");
	words.add("judicious");
	words.add("taut");
	words.add("smolder");
	
		
			System.out.println(words);
		
	
	Map<String, String> dictionary = new HashMap<>();
	dictionary.put("disallow", "refuse to allow or accept as a correct");
	dictionary.put("discourse", "speech lecture");
	dictionary.put("fidelity", "loyalty accuracy");
	dictionary.put("muffler", "cloth worn round the neck/silencer");
	dictionary.put("limn", "paint portray");
	dictionary.put("myriad", "very great number");
	dictionary.put("disconcert", "upset the self-possession of");
	dictionary.put("judicious", "sound in judgment wise");
	dictionary.put("taut", "tightly stretched");
	dictionary.put("smolder", "burn slowly without flame");
	
		System.out.println(dictionary);
	
	}
}
