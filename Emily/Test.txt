public class Dish {
	String name;
	int percentage;
	String link;
	String[] ingredients;
	

	public Dish(String namePassedIn, int percentagePassedIn, String linkPassedIn, String[] ingredientsPassedIn) {
		this.name = namePassedIn;
		this.percentage = percentagePassedIn;
		this.link = linkPassedIn;
		this.ingredients = ingredientsPassedIn;
	}

	public void displayAllInfo() {
    	System.out.println("Name: " + this.name);
    	System.out.println("Percentage: " + this.percentage);
    	System.out.println("Link: " + this.link);
    	System.out.println("Ingredients: ");
    	for (int i = 1; i < ingredients.length; i++) {
	
    	System.out.print(ingredients[i] + ", ");
    	}
    	
  }
	
	public void caculatePercentage(String[] keywords) {
	int count = 0;
	float percentage = 0;
	for (int i = 0; i < keywords.length; i++) {
		for (int x = 0; x < ingredients.length; x++) {
			if(keywords[i] == ingredients[x]) {
				count = count + 1;
			}
			}
		}
		percentage = ((float) count / ((float) ingredients.length)) * 100;
       		this.percentage = (int) percentage;

	}
}

//Teams Code Homework:

public class TeamsCode {

	public static void main(String[] args) {
		try {
			Scanner scanner = new Scanner(System.in);

			System.out.printf("Please enter input:\n");

			List<String> inputList = new ArrayList<>();
			String line;

			while (scanner.hasNextLine()) {
				line = scanner.nextLine();
				inputList.add(line);
			
			}

			scanner.close();

			String[] inputArray = new String[inputList.size()];
			inputArray = inputList.toArray(inputArray);

			for (int i = 1; i < inputArray.length; i++) {
				String str = inputArray[i];
				String[] strSplit = str.split(" ");
				String strNum1 = strSplit[0];
				String strNum2 = strSplit[1];
				int intNum1 = Integer.parseInt(strNum1);
				int intNum2 = Integer.parseInt(strNum2);
				if (intNum1 * intNum2 > intNum1 + intNum2) {
					System.out.println(intNum1 * intNum2);
					
				} else {
					System.out.println(intNum1 + intNum2);

				}
			}
		

		} catch (Exception e) {
		}

	}

}

// Homework for 12/11/2020:
// Part 1
public class TeamCode {

	public static void main(String[] args) {

		try {
			Scanner scanner = new Scanner(System.in);
			List<String> inputList = new ArrayList<>();
			String line;
			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}
			scanner.close();
			String[] inputArray = new String[inputList.size()];
			inputArray = inputList.toArray(inputArray);

			for (int i = 0; i < inputArray.length; i++) {
				String str = inputArray[i];

			int intNum1 = Integer.parseInt(str);
			int sum = 0;
			for (int j = 0; j < intNum1; j++) {
				sum = sum + Integer.parseInt(strSplit[j]);
				}
				System.out.println(sum);
			}

		} catch (Exception e) 
		{
	
		}
	}

// Part 2:
public class DishTest {

	public static void main(String[] args) {
		String[] keywords = { "Eggs", "Cheese", "Garlic" };

		Dish alfredo = new Dish("Alfredo", 0, "https://www.modernhoney.com/fettuccine-alfredo/",
				new String[] { "Pasta noodles", "Butter", "Heavy Cream", "Garlic", "Parmesan Cheese", "Black Pepper" });
				
		Dish friedRice = new Dish("Fried Rice", 0,
				"https://www.allrecipes.com/recipe/79543/fried-rice-restaurant-style", new String[] { "Eggs", "Garlic",
						"Rice", "Scallion", "Onion", "Carrot", "Soy sauce", "Peas", "Sesame oil", "Vegetable Oil" });
				
		Dish grilledCheeseSandwich = new Dish("Grilled Cheese Sandwich", 0,
				"https://www.allrecipes.com/recipe/23891/grilled-cheese-sandwich/",
				new String[] { "Cheese", "Bread", "Butter" });
				
		Dish omelette = new Dish("Omelette", 0,
				"https://www.delish.com/cooking/recipe-ideas/a24892843/how-to-make-omelet/",
				new String[] { "Cheese", "Butter", "Black Pepper", "Ham", "Milk", "Eggs", "Salt" });
				
		Dish spaghetti = new Dish("Spaghetti", 0,
				"https://www.foodnetwork.com/recipes/food-network-kitchen/simple-spaghetti-with-tomato-sauce-3362665",
				new String[] { "Pasta noodles", "Tomatoes", "Tomato sauce", "Cheese", "Garlic", "Basil", "Onion",
						"Ground Beef", "Black pepper", "Olive oil", "Salt" });

		Dish[] arrayDishes = new Dish[5];
		arrayDishes[0] = alfredo;
		arrayDishes[1] = friedRice;
		arrayDishes[2] = grilledChesseSandwich;
		arrayDishes[3] = omelette;
		arrayDishes[4] = spaghetti;

		for (int i = 0; i < arrayDishes.length - 1; i++) {
			arrayDishes[i].caculatePercentage(keywords);

		}

		sort(arrayDishes);

		for (int i = 0; i < arrayDishes.length - 1; i++) {
			arrayDishes[i].displayNameAndPercentag();
		}

	}
	
	private static void sort(Dish[] arrayDishes) {
	int leng = arrayDishes.length;
	for (int i = 0; i < leng - 1; i++) {
		for (int j = 0; j < leng - i - 1; i++) {
			int x = arrayDishes[j][1];
			int y = arrayDishes[j + 1][1];
			if (x < y) {
				Dish temp = arrayDishes[j];
				arrayDishes[j] = arrayDishes[j + 1];
				arrayDishes[j + 1] = temp;
				}

			}
		}
	}

// Homework for 12/18/2020:
// Part 1
public class rainbow {

	public static void main(String[] args) {

		List<String> rainbow = new ArrayList();
		rainbow.add("red");
		rainbow.add("orange");
		rainbow.add("yellow");
		rainbow.add("green");
		rainbow.add("blue");
		rainbow.add("purple");
		for (int i = 0; i < rainbow.size(); i++) {
			System.out.println(rainbow.get(i));
		}
	}
}

// Part 2
public class teamscode {

	public static void main(String[] args) {
		try{
			Scanner scanner = new Scanner(System.in);       
			
			List<String> inputList = new ArrayList<>();
			String line;
			
			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}
			
			scanner.close();
			
			String[] inputArray = new String[inputList.size()]; 
			inputArray = inputList.toArray(inputArray);
			
			String[] strSplit = inputArray[1].split(" ");
			int sum = 0;
			char firstCh = inputArray[0].charAt(0);
			System.out.println(firstCh);
			for(int i = 0; i < strSplit.length; i++) {
				if(firstCh == strSplit[0].charAt(i));
				sum = sum + 1;
			}
			System.out.println(sum);
			
	    }
	    catch(Exception e) {
	    	
	    }
	}
