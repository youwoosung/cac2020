import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class randomizer {
	public static void main(String[] args) {
	
			Scanner scanner = new Scanner(System.in);

			List<String> inputList = new ArrayList<>();
			String line;

			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}

			scanner.close();

			String[] inputArray = new String[inputList.size()];
			inputArray = inputList.toArray(inputArray);
			for (int i = 0; i < inputArray.length; i++) {
				String str = inputArray[i];
				String[] strSplit = str.split(" ");
			int min =  Integer.parseInt(strSplit[0]);
			int max =  Integer.parseInt(strSplit[1]);
			
			int randomNumber = (int) (Math.random() * (max-min+1) + min);
			System.out.println(randomNumber);
		
	}
}
}

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class medium1 {  i couldnt figure it out
	public static void main(String[] args) {
		try {
			Scanner scanner = new Scanner(System.in);

			List<String> inputList = new ArrayList<>();
			String line;

			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}

			scanner.close();

			String[][] inputArray = new String[inputList.size()][inputList.size()];
			inputArray = inputList.toArray(inputArray);

			int rows = Integer.parseInt(inputArray[0][0]);
			int columns = Integer.parseInt(inputArray[1][0]);
			int[][] newArray = new int[rows][columns];

			String[] array = new String[inputList.size()];

			for (int i = 1; i < inputArray.length; i++) {
				for (int j = 1; j < inputArray.length; j++) {
					String str = (inputArray[i][0]);
					String str1 = (inputArray[0][i]);
					String[] strSplit = str.split(" ");
					String[] strSplit1 = str1.split(" ");
					System.out.println(strSplit);
				}
				
			}

		} catch (Exception e) {

		}
	}
}
