import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class easy5 {

	public static void main(String[] args) {
		try {
			Scanner scanner = new Scanner(System.in);

			List<String> inputList = new ArrayList<>();
			String line;

			while (scanner.hasNext()) {
				line = scanner.nextLine();
				inputList.add(line);
			}

			scanner.close();

			String[] inputArray = new String[inputList.size()];
			inputArray = inputList.toArray(inputArray);
			
			
			for (int i = 1; i < inputArray.length; i++) {
				int count = 0;
				int sum = 0;
				int num = Integer.parseInt(inputArray[i]);
			while (num > 0) {
				int digit = num % 10;
				if (count % 2 == 0) {
					sum += digit;
					num /= 10;
					count++;
				}
			}
			System.out.println(sum);
			}
//			char firstCh = inputArray[0].charAt(0);
//			System.out.println(firstCh);
			
//			for (int i = 0; i < inputArray.length; i++) {
//				int sum = Integer.parseInt(inputArray[0]);
//				count += sum % 10;
//				sum /= 100;
//				System.out.println(sum);
//			}

		} catch (Exception e) {

		}

	}
}
package dishes;

import java.util.HashMap;
import java.util.HashSet;

public class Hash {

	public static void main(String[] args) {

		HashSet<String> words = new HashSet<>();
		words.add("disallow");
		words.add("discourse");
		words.add("fidelity");
		words.add("muffler");
		words.add("limn");
		words.add("myriad");
		words.add("disconcert");
		words.add("judicious");
		words.add("taut");
		words.add("smolder");
		
		words.forEach(System.out :: println);
		
		HashMap<String, String> dictionary = new HashMap<>();
		dictionary.put("disallow", "refuse to allow or accept as a correct");
		dictionary.put("discourse", "speech lecture");
		dictionary.put("fidelity", "loyalty");
		dictionary.put("muffler", "cloth worn round the neck/silencer");
		dictionary.put("limn", "paint portray");
		dictionary.put("myriad", "very great number");
		dictionary.put("disconcert", "upset the self-possesion of");
		dictionary.put("judicious", "sound in judgement wise");
		dictionary.put("taut", "tightly stretched");
		dictionary.put("smolder", "burn slowly without flame");
		
		
		
		System.out.println(dictionary);
//		for(int i = 0; i < dictionary.size(); i++) {
//			String key = dictionary.getKey();
//			System.out.println()
//		}
	}
	
}

package dishes;

import java.util.HashMap;
import java.util.HashSet;

public class Hash {

	public static void main(String[] args) {

		HashSet<String> words = new HashSet<>();
		words.add("disallow");
		words.add("discourse");
		words.add("fidelity");
		words.add("muffler");
		words.add("limn");
		words.add("myriad");
		words.add("disconcert");
		words.add("judicious");
		words.add("taut");
		words.add("smolder");
		words.add("awesome");
		words.add("tall");
		
		words.forEach(System.out :: println);
		
		HashMap<String, String> dictionary = new HashMap<>();
		dictionary.put("disallow", "refuse to allow or accept as a correct");
		dictionary.put("discourse", "speech lecture");
		dictionary.put("fidelity", "loyalty");
		dictionary.put("muffler", "cloth worn round the neck/silencer");
		dictionary.put("limn", "paint portray");
		dictionary.put("myriad", "very great number");
		dictionary.put("disconcert", "upset the self-possesion of");
		dictionary.put("judicious", "sound in judgement wise");
		dictionary.put("taut", "tightly stretched");
		dictionary.put("smolder", "burn slowly without flame");
		dictionary.put("awesome", "what luke is not");
		dictionary.put("tall", "what luke definitely is not");
		
		
		System.out.println(dictionary);
//		for(int i = 0; i < dictionary.size(); i++) {
//			String key = dictionary.getKey();
//			System.out.println()
//		}
	}
	
}

